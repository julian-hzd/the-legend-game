
import GameEntity from '../GameEntity.js'

export default class Gobli extends GameEntity{

    constructor(dimensions, position, level, player){
		super(dimensions, position, null, level);

    }

    
}